# use this commands to apply the changes

git status

git add .

git commit -m "Organized files into styles, scripts, and images folders"

git push origin main

still learning css and html structure...
